#!/usr/bin/env php
<?php

require __DIR__.'/../vendor/autoload.php';

use Coach\Command;
use Symfony\Component\Console\Application;

$application = new Application('Coach', '0.1-dev');
$application->add(new Command\GreetCommand());
$application->run();
